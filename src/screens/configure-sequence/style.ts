import styled from "styled-components/native";
import { Container } from "components/Container";

export const ConfigureSequenceContainer = styled(Container)``;

export const ConfigureContainer = styled(Container)`
  flex: 8;
  align-items: center;
  justify-content: space-around;
`;
